import FleetClient from './FleetClient';

export const metadata = {
  title: "Fleet Ledger — Rydex",
  description: "A mechanical catalogue of every Rydex vehicle class. Bike to 10-tonne freight, dispatched across Srinagar.",
};

export default function FleetPage() {
  return <FleetClient />;
}
