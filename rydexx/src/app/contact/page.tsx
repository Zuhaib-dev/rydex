import ContactClient from './ContactClient';

export const metadata = {
  title: "Field Office — Rydex Dispatch",
  description: "Raise a dispatch ticket with Rydex HQ at Residency Road, Srinagar. Live network operational 24/7.",
};

export default function ContactPage() {
  return <ContactClient />;
}
